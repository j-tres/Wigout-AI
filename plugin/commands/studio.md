---
description: Wigout Studio front desk — describe what you need; the right specialist takes it
argument-hint: "[request — or 'setup' to install/verify the environment]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/front-desk/SKILL.md` and follow it.

Request: $ARGUMENTS

If the request is `setup`, run the environment setup flow. If empty,
give a one-paragraph studio tour (roles + example asks) grounded in
whether the bridge is currently reachable.
