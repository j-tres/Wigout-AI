---
description: Mix review & fixes — measured report card, verified parameter moves, optional render-verify loop
argument-hint: "[what to work on — e.g. 'balance the chorus' or 'bass vs kick']"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/engineer/SKILL.md` and follow it in
mix posture (balance, gain staging, masking, space). Full mix authority
with verified writes; render-verify when a render is available or asked
for.

Request: $ARGUMENTS
