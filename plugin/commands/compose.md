---
description: Summon the Composer — write MIDI, build tracks, convert clips to MIDI, generate audio
argument-hint: "[what to compose — e.g. 'a bassline for the chorus' or 'convert my selected clip to MIDI with violin']"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/composer/SKILL.md` and follow it.

Request: $ARGUMENTS

If empty, snapshot the project and ask what to compose, offering 2-3
concrete suggestions that fit what's already there.
