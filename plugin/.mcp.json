{
  "mcpServers": {
    "Wigout-MCP": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project", "${CLAUDE_PLUGIN_ROOT}/scripts",
        "python", "${CLAUDE_PLUGIN_ROOT}/scripts/mcp_stdio_bridge.py"
      ]
    }
  }
}
